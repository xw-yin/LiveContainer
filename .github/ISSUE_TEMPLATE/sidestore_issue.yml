name: Built-in SideStore Bug Report
description: Report a bug when using LiveContainer's built-in SideStore.
title: "[BUG] "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to fill out this bug report! Before you continue filling out the report, please **[read our FAQ](https://github.com/LiveContainer/LiveContainer/issues/265) and [search in GitHub Issues](https://github.com/LiveContainer/LiveContainer/issues) and also search for similar issues in SideStore's repo / docs for the bug you are experiencing.**

  - type: textarea
    id: description
    attributes:
      label: Describe the bug
      description: What is the bug and how did you discover it?
      placeholder: Please be concise with your description.
    validations:
      required: true
  - type: textarea
    id: how-to-reproduce
    attributes:
      label: Instructions to reproduce
      description: Please include consistent instructions for reproducing the bug.
    validations:
      required: true
  - type: input
    id: app-version
    attributes:
      label: What version of LiveContainer and iOS are you using?
      description: To retrieve LiveContainer version, go to LiveContainer settings, then scroll to the bottom of the page.
    validations:
      required: true
  - type: checkboxes
    attributes:
      label: Verify steps
      options:
        - label: I downloaded LiveContainer from releases / actions section or LiveContainer's AltStore source.
        - label: I have the latest LiveContainer.
        - label: I installed LiveContainer through AltStore/SideStore/Impactor/iloader/TrollStore or I'm confident that I signed LiveContainer correctly with all required entitlements.
    validations:
      required: true
  - type: textarea
    id: crash-log
    attributes:
      label: Crash Log
      description: If a crash happened, please upload the crash log in iOS settings - Privacy Security - Analytics Improvements - Analytics Data - Search LiveContainer/LiveProcess. **Do not copy and paste.** Instead, please change .ips to .txt and upload it. If you are certain no crash report is produced, type "no crash report is produced".
    validations:
      required: true
  - type: textarea
    id: sidestore-error-message
    attributes:
      label: Error Message
      description: Tell us the exact error message you see when it failed to refresh / login etc.
    validations:
      required: true
  - type: textarea
    id: sidestore-health-check
    attributes:
      label: Health Check Screen
      description: If you are using LiveContainer+SideStore 3.7.13+, please Send the "Health Check" page in sidestore settings. Be sure to scroll from top to bottom. If you are using an older version, please upgrade first.
    validations:
      required: true
  - type: textarea
    id: sidestore-console-logs
    attributes:
      label: Console Logs
      description: Please send sidestore's console log. First trigger the bug, then find it in LiveContainer's folder in "Files" app -> sidestore -> Documents -> ConsoleLogs -> upload the most recent one.
    validations:
      required: true
  - type: textarea
    id: other-info
    attributes:
      label: Other info
      description: If you have any other info that might be useful, or if you found a workaround, please put it here.
